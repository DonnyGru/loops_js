const x = 9;
for (i = 0; i < 10; i++){
    const result = x * i;
    console.log(i + "*" + x + "=" + result);
}

//Bonus
for (y = 0; y <= 10; y++) {
    for ( a = 0; a <= 10; a++) {
      const result = y * a;
      console.log(y + " * " + a + " = " + result);
    }
  }